// *Good testcase*
// Declaring and initializing multiple variables in one variable declaration
// Should compile
// Expected result: 6

int main() {
    int a = 1, b = 2, c = 3;
    return ((a +b) + c);
}