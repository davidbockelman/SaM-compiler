// testing break in loop
// Should compile.
// Expected result: 5

int main() {
    int a;
    a = 0;
    while ((a < 10)) {
        a = (a + 1);
        if ((a = 5)) {
            break;
        } else;
    }
    return a;
}