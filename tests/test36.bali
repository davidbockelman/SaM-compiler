// simple test for x86 generation
// should compile
// Expected output=8

int main() {
    int c = (-1);
    int i = 0;
    while ((i < 2)) {
        c = (c + i);
        i = (i + 1);
    }
    return c;
}