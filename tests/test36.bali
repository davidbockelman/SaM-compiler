// *Good testcase*
// simple test for x86 generation
// should compile
// Expected result: 55

int main() {
    return fib(9);
}

int fib(int n) {
    if (((n=0) | (n=1))) {
        return 1;
    } else {
        return (fib((n-1)) + fib((n-2)));
    }
}